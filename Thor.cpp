#include <bits/stdc++.h>

#define lli long long int
#define ull unsigned long long int
#define mod 1000000007
#define pq priority_queue
#define pb push_back
#define ld long double
#define REP(x,n) for (int x = 0; x < n; x++)

using namespace std;

class Solution {
    public:
        

};

int main() {

#ifndef ONLINE_JUDGE
    freopen("Input1.txt","r", stdin);
    freopen("Output1.txt","w", stdout);
#endif
    lli n, q;
    cin >> n >> q;
    int noti = 0;
    int sum = 0;
    int read = 0;
    vector<int> v(n + 1, 0);
    vector<vector<int>> tm(q, vector<int>(n, 0));
    int mxRead = 0;
    REP(x, q) {
        lli t;
        cin >> t;
        if(t==)

    }
    return 0;
}